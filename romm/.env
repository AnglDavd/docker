# ------------------------------------------------------------------------------
# ROMM Environment Variables Configuration
#
# This file defines environment variables for the ROMM (Rom Manager) Docker Compose setup.
# These variables are essential for configuring database connections, authentication,
# and various metadata providers.
#
# IMPORTANT: Replace all placeholder values (e.g., empty strings, 'your_secret_key')
# with your actual, secure credentials and API keys.
# ------------------------------------------------------------------------------

# Database connection details for the ROMM application.
# These variables are used by the ROMM application to connect to the MariaDB database.
DB_HOST=romm-db       # Hostname of the database service (as defined in compose.yaml)
DB_NAME=romm          # Name of the database for ROMM
DB_USER=romm-user     # Username for the database connection
DB_PASSWD=            # 🚨 IMPORTANT: Database password for the ROMM user. Must be set!

# ROMM Application Security
ROMM_AUTH_SECRET_KEY= # 🔑 IMPORTANT: Generate a unique secret key for authentication.
                      # You can use `openssl rand -hex 32` on Linux/macOS to generate one.
                      # This key is crucial for securing user sessions and API calls.

# Metadata Providers Configuration
# These API keys and credentials are used by ROMM to fetch game metadata, box art, and more.
# Refer to the official ROMM documentation for detailed setup instructions for each provider.

SCREENSCRAPER_USER=     # Username for ScreenScraper.net. Recommended metadata provider.
SCREENSCRAPER_PASSWORD= # Password for ScreenScraper.net.
                        # More info: https://docs.romm.app/latest/Getting-Started/Metadata-Providers/#screenscraper

RETROACHIEVEMENTS_API_KEY= # API Key for RetroAchievements.org.
                           # More info: https://docs.romm.app/latest/Getting-Started/Metadata-Providers/#retroachievements

STEAMGRIDDB_API_KEY= # API Key for SteamGridDB.com (for game artwork).
                     # More info: https://docs.romm.app/latest/Getting-Started/Metadata-Providers/#steamgriddb

HASHEOUS_API_ENABLED=true # Enable/Disable Hasheous API integration.
                          # More info: https://docs.romm.app/latest/Getting-Started/Metadata-Providers/#hasheous

# Base path for ROMM data volumes (game library, assets)
ROMM_DATA_BASE=/media/data/Docker/romm # 📂 Base directory on the host for ROMM's persistent data

# MariaDB Database Service Configuration
# These variables are used by the MariaDB Docker container to initialize the database.
MARIADB_ROOT_PASSWORD= # 🔒 IMPORTANT: Set a strong, unique root password for MariaDB.
MARIADB_DATABASE=romm  # Database name to be created (should match DB_NAME above)
MARIADB_USER=romm-user # User to be created with access to MARIADB_DATABASE (should match DB_USER above)
MARIADB_PASSWORD=      # 🚨 IMPORTANT: Password for the MARIADB_USER. Must be set and match DB_PASSWD above!